server:
  port: 9090

spring:
  datasource:
    url: jdbc:postgresql://db:5432/stats
    username: sa
    password: sa
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
  sql:
    init:
      mode: always